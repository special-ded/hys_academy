export enum AlbumEnum {
  Album1 = 'Album 1',
  Album2 = 'Album 2',
  Album3 = 'Album 3'
};

export enum AppEnum {
  key = 'localStorageSliderData',
  select = "#select",
  paginator = "#paginator",
  slider = "#slider",
  customerSlider = "#customer-slider",
  header = '.header'
}

export enum FormAlertEnum {
  userNameAlert = 'Please input valid Name & Surname',
  telephoneAlert = 'Format: +38 077 777 77 77',
  emailAlert = 'Invalid email given'
}

export enum FormSelector {
  form = 'form',
  username = '#username',
  telephone = '#telephone',
  email = '#email',
  userAlert = '#username__alert',
  telephoneAlert = '#telephone__alert',
  emailAlert = '#email__alert',
  alert = 'form__alert'
}

